language: python
cache: apt
sudo: false
branches:
  only:
  - master
python:
- 3.6

install:
- pip install -U --upgrade-strategy=eager 'Nikola[extras]'

script:
  - nikola build

deploy:
  provider: script
  skip_cleanup: true
  script: bash deploy.sh
